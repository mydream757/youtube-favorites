import React from 'react';
import './Loading.scss';

const Loading = () => {
    return (
        <div id="loading-wrap">
            <div id="loading">Loading...</div>
        </div>
    );
};

export default Loading;